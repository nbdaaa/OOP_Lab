1. JAVA doesn't allow us to  create a constructor method to build a DVD by title then create a constructor method to build
a DVD by category
<-- This is because two methods have the same input variable which means JAVA considers them as the same method.